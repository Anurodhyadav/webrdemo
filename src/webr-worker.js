// // importScripts("https://cdn.webr.workers.dev/latest/webr-serviceworker.js");

// // onmessage(({ data }) => {
// //   console.log({ data });
// // });

// let counter = 0;

// self.postMessage(counter++);
// setInterval(() => {
//   self.postMessage(counter++);
// }, 5000);

import("https://cdn.webr.workers.dev/latest/webr-worker.js");
